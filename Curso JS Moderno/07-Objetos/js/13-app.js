// Object Literal
const producto = {
    nombre: "Monitor de 20 Pulgadas",
    precio: 300,
    disponible: true
}

console.log(Object.keys(producto)); // Retorna llaves

console.log(Object.values(producto)); // Retorna valores

console.log(Object.entries(producto)); // Retorna ambos